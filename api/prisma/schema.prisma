generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Netflix {
  id Int @default(autoincrement()) @id @unique
  netflixName String @unique
  imdbId String @unique
  imdbRating String
  imdbVotes String
  metaScore String
  type String
  createdAt DateTime @default(now())
  updatedAt DateTime @default(now())
}